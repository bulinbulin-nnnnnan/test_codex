1. 每个创建的结构体的id字段都是唯一的且纯数字类型
2. 每个新建的函数都要注释函数的功能和参数，创建人邮箱（wangqingsong.odcn@bytedance.com）和创建时间
3. 每个新建的函数都要测试异常参数的情况，如：0、空字符串、负数等
4. 每个新建的函数都要测试边界值的情况，如：最大值、最小值等
5. 每个新建的函数都要测试性能问题，如：超时、内存泄漏等，函数声明前注释性能参数（如：本地平均运行时间、内存占用等）